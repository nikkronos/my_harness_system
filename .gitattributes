# Окончания строк: git решает сам для текстовых файлов
* text=auto

# Скрипты и git-хуки — всегда LF.
# Без этого на Windows хук получает CRLF и не запускается.
*.sh          text eol=lf
.githooks/**  text eol=lf
