---
description: Начало сессии — агент входит в контекст и разбирается в задаче, ничего не меняя.
argument-hint: [задача одной строкой; можно не писать и объяснить следующим сообщением]
---
Привет.

`CLAUDE.md` у тебя уже загружен. Прочитай `Main_docs/PROFILE.md` — кто я и как со мной работать.

Дальше по задаче:

- касается проекта → найди его в `Main_docs/PROJECTS.md`, открой файлы проекта;
- касается моей жизни, а не только кода → загляни в `KB/` (нужный файл, не всю папку).

Режим: **пока ничего не меняй.** Сначала скажи:

- что понял;
- что факт, а что твоё предположение;
- что может пойти не так;
- минимальный план;
- какие вопросы реально мешают начать (остальные не задавай).

После этого работай сам, без пошагового согласования.

Задача: $ARGUMENTS
